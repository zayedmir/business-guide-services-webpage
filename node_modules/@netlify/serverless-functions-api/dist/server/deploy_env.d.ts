/** Records the path of the deploy's environment variables file. */
export declare const setDeployEnvPath: (path: string) => void;
/**
 * Records where the host writes the certificate authority we should trust.
 */
export declare const setHostCAPath: (path: string) => void;
/** @internal Resets the injection state. Exported for testing only. */
export declare const resetInjectedDeployEnv: () => void;
export declare const injectDeployEnvVars: (env?: NodeJS.ProcessEnv) => Promise<void>;
