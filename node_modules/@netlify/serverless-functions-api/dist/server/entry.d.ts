import { Server as HTTPServer, type IncomingMessage, type ServerResponse } from 'node:http';
export type NodeListener = (req: IncomingMessage, res: ServerResponse) => void;
export interface DrainOptions {
    graceMs: number;
}
export type EntryKind = 'function' | 'server';
export declare const SERVER_MARKER_FILENAME = "___netlify-server.json";
export declare const detectEntryKind: (entryPath: string) => Promise<EntryKind>;
export type EntryForm = 'handler' | 'fetch' | 'listener' | 'adopted' | 'compat';
export interface ServerEntry {
    form: EntryForm;
    listener?: NodeListener;
    server?: HTTPServer;
    drain: (options: DrainOptions) => Promise<void>;
}
export interface ResolveServerEntryOptions {
    readinessTimeoutMs?: number;
}
export declare const resolveServerEntry: (entryPath: string, { readinessTimeoutMs }?: ResolveServerEntryOptions) => Promise<ServerEntry>;
export declare const dispatchEntryRequest: (entry: ServerEntry, req: IncomingMessage, res: ServerResponse) => void;
