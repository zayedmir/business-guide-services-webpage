import { InvocationMetadata } from './metadata.js';
interface EnvironmentVariables {
    directory: string;
    encryptionKey: string;
}
interface EnvironmentOptions {
    aiGateway?: string;
    blobs?: string;
    environmentVariables?: EnvironmentVariables;
    env?: NodeJS.ProcessEnv;
    headers: Headers;
    invocationMetadata?: InvocationMetadata;
    netlifyDBURL?: string;
    purgeAPIToken?: string;
}
export declare const setupEnvironment: ({ aiGateway, blobs, environmentVariables, env, headers, invocationMetadata, netlifyDBURL, purgeAPIToken, }: EnvironmentOptions) => Promise<void>;
/**
 * Whether the deploy ships an encrypted env vars file in the given directory.
 */
export declare const hasEncryptedEnvVars: (directory: string) => boolean;
/**
 * Reads and decrypts the encrypted env vars file at the given path and injects
 * them into the environment. Skips if the env vars have already been injected.
 */
export declare const injectEncryptedEnvVars: (processEnv: NodeJS.ProcessEnv, directory: string, encryptionKey: string) => Promise<void>;
/** @internal Resets the injection state. Exported for testing only. */
export declare const resetInjectedEnvVars: () => void;
export {};
