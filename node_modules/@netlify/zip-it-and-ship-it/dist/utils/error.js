const createFunctionsBundlingErrorInfo = (location) => ({
    type: 'functionsBundling',
    location,
});
export class FunctionBundlingUserError extends Error {
    customErrorInfo;
    constructor(message, customErrorInfo) {
        super(message);
        Object.setPrototypeOf(this, new.target.prototype);
        this.name = 'FunctionBundlingUserError';
        Error.captureStackTrace(this, FunctionBundlingUserError);
        this.customErrorInfo = createFunctionsBundlingErrorInfo(customErrorInfo);
    }
    static addCustomErrorInfo(error, customErrorInfo) {
        ;
        error.customErrorInfo = createFunctionsBundlingErrorInfo(customErrorInfo);
        return error;
    }
}
